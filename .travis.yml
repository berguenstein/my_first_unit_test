language: c
stages:
  - compile
  - test
jobs:
  include:
    -stage: compile
      before_script:
        - cd HW_behaviour
      script:
        - gcc main.c operations.c -o outputRun
    -stage: test
      script:
        - ./outputRun
